/**
 * Preparation commune des tests.
 *
 * `fake-indexeddb/auto` installe une implementation d'IndexedDB en memoire :
 * les depots sont donc testes reellement, sans navigateur et sans mock maison.
 * Licence MIT.
 */

import 'fake-indexeddb/auto';
