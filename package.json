{
  "name": "agilmea-ik",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Agilmea IK â€” suivi des frais kilometriques multi-structures (PWA locale, sans backend).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build",
    "release:patch": "node scripts/release.mjs patch",
    "release:minor": "node scripts/release.mjs minor",
    "release:major": "node scripts/release.mjs major"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "vite": "^6.4.3",
    "vitest": "^4.1.10"
  }
}

